/**
 * Prompt moved to [src/shared/planner/prompts/queryPlanSystem.ts](../../../shared/planner/prompts/queryPlanSystem.ts)
 * so the web bundle can reuse it. This file is kept as a re-export to avoid
 * breaking legacy imports inside the main process.
 */
export { queryPlanSystemPrompt } from '@shared/planner';
