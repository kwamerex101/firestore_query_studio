/**
 * Prompt moved to [src/shared/planner/prompts/insightsSystem.ts](../../../shared/planner/prompts/insightsSystem.ts)
 * so the web bundle can reuse it. This file is kept as a re-export to avoid
 * breaking legacy imports inside the main process.
 */
export { insightsSystemPrompt } from '@shared/planner';
